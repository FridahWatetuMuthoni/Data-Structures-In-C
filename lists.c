#include <stdio.h>
#include <stdlib.h>

/*
LIST:(Dynamic List Creation)
-> Stores a given number of elements of a given data type
-> Write and modify an element at a given position
->Read element at  a given poistion
FEATURES:
->The size of list should be zero when empty
->Can insert an element
->Can Remove an element
->Can count the number of elements in the list
->Can read and modify an element at a position
->Can specify the data-type
*/

int main(void)
{
    return (0);
}